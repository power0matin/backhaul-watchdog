# Backhaul Watchdog Configuration
PING_TARGETS="8.8.8.8 1.1.1.1"  # List of ping targets (IP or domain)
MAX_LATENCY=200                  # Maximum allowed latency in milliseconds
CHECK_INTERVAL=30                # Check interval in seconds
SERVICE_NAME="backhaul"          # Service to restart
COOLDOWN=300                     # Cooldown after restart in seconds